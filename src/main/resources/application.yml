spring:
  application:
    name: groups-service
  rabbitmq:
    host: woodpecker.rmq.cloudamqp.com
    port: 5671
    username: qdockgcf
    password: CtfBnZjtftQEOyAEBWhhgdFpft9ONruW
    virtual-host: qdockgcf
    ssl:
      enabled: true

server:
  port: ${PORT:8082}

eureka:
  client:
    service-url:
      defaultZone: https://discovery-server-synhub.onrender.com/eureka/
  instance:
    prefer-ip-address: true